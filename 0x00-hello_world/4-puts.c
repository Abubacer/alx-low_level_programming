#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char Puzzle[] = "\"Pogramming is like building a multilingual puzzle";

	puts(Puzzle);

return (0);
}
